# Backend Update

Updated:
- GET /api/v1/personalized/path
- GET /api/v1/gamification

Both endpoints remain backward-compatible with the existing FE and derive their values from existing progress, enrollment, quiz, flashcard and game collections. No MongoDB migration is required.
