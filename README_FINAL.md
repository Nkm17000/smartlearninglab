Smart Learning Lab BE — FINAL REGISTRATION

Includes email confirmation registration, duplicate-email 409 handling,
Gmail SMTP/App Password configuration, logging and existing APIs.

Run:
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
